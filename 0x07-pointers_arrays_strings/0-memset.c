#include "main.h"
/**
 * memset - fills n bytes of memory
 *
 * @s: pointer to start of memory area
 * @c: constant byte to fill with
 * @n: num bytes to fill in
 *
 * Return: num bytes to fill in
 */
char *_memset(char *s, char b, unsigned int n)
{
         unsigned int i = 0;
	 unsigned char *memory = s, value = c;

	while (i < n)
       	{
		*s++ = b;
		i++;
	}
	return (start);
}

